# EasyDict Docker 环境变量配置
# 复制本文件为 .env 并修改对应的值：cp .env.example .env

# 词典数据根目录路径（服务器上的实际路径）
# 目录结构示例:
# /data/dictionaries/
# ├── dictid653/
# │   ├── dictionary.db
# │   └── metadata.json
# └── dictid654/
#     └── ...
DICTIONARIES_PATH=/data/dictionaries

# 辅助数据文件目录路径（可选）
# 用于存储辅助数据文件，如 en.db 等
# 目录结构示例:
# /data/auxiliary/
# ├── en.db
# ├── cn.db
# └── ...
# AUXILIARY_PATH=/data/auxiliary

# 用户数据持久化目录（存储用户数据库、设置等，运行时自动生成）
# 通过 docker-compose.yml 中的 volumes 配置挂载，无需在此设置

# JWT 签名密钥（可选）
# 用于对用户登录 Token 进行签名，不填则每次启动随机生成
# 设置后服务重启不影响用户登录状态，可用以下命令生成随机值：
#   openssl rand -hex 32
# JWT_SECRET=

# API 服务端口（可选，默认 8080）
# API_PORT=8080

# 日志级别（可选，默认 info）
# LOG_LEVEL=info
