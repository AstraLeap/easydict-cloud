# EasyDict Docker 环境变量配置
# 复制本文件为 .env 并修改对应的值：cp .env.example .env

# 词典数据根目录路径（服务器上的实际路径）
# 目录结构示例:
# /data/dictionaries/
# ├── dictid653/
# │   ├── dictionary.db
# │   ├── audios/
# │   │   └── example.mp3
# │   └── images/
# │       └── example.png
# └── dictid654/
#     └── ...
DICTIONARIES_PATH=/data/dictionaries

# 辅助数据文件目录路径（可选）
# 用于存储辅助数据文件，如 en.db 等
# 目录结构示例:
# /data/auxiliary/
# ├── en.db
# ├── cn.db
# └── ...
# AUXILIARY_PATH=/data/auxiliary

# 贡献者平台数据目录（存储用户数据库等持久化数据）
CONTRIBUTOR_DATA_PATH=/data/contributor

# 贡献者平台 Flask Session 密钥（必须修改为随机字符串！）
CONTRIBUTOR_SECRET_KEY=change_me_to_a_random_secret_string

# API 服务端口（可选，默认 8080）
# API_PORT=8080

# 日志级别（可选，默认 info）
# LOG_LEVEL=info
